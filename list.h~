#ifndef LIST_H
#define LIST_H

#include "itemtype.h"

class List {

 public:
  struct FileNode {
    File* fileInfo = NULL;
    FileNode* nextFile = NULL;
  };
  List();
  void insertFile(File* file);
  FileNode* getFileHead();

 private:
  FileNode* fileHead;

};

#endif
